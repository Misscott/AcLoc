<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AcLoc" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AcLoc" parent="Base.Theme.AcLoc" >
        <item name="colorPrimary">@color/primaryColor_light</item>
        <item name="colorPrimaryVariant">@color/primaryVariant_light</item>
        <item name="colorOnPrimary">@color/onPrimaryColor_light</item>
        <item name="colorSecondary">@color/onSecondaryColor_light</item>
        <item name="android:colorBackground">@color/backgroundColor_light</item>
        <item name="colorSurface">@color/surfaceColor_light</item>
        <item name="android:textColorPrimary">@color/onBackgroundColor_light</item>
        <item name="colorOnSurface">@color/onSurfaceColor_light</item>  <!-- Ensure this is set -->
        <item name="colorError">@color/errorColor_light</item> <!-- Error Color -->
        <item name="colorAccent">@color/accentColor_light</item> <!-- Accent Color -->
        <item name="backgroundColor">@color/backgroundColor_light</item>
        <item name="colorTertiary">@color/colorTertiary_light</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
        <item name="behavior_peekHeight">@dimen/bottom_sheet_peek_height</item>
        <item name="behavior_hideable">true</item>
        <item name="behavior_skipCollapsed">false</item>
        <item name="behavior_fitToContents">false</item>
    </style>

    <item name="bottom_sheet_peek_height" type="dimen">50%</item>
</resources>
